import { addNumber } from '../../../helpers/add-number';
import { module, test } from 'qunit';

module('Unit | Helper | add number');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = addNumber(42);
  assert.ok(result);
});
